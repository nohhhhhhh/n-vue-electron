import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  /* state 값을 변경 하는 이벤트 로직(동기) */
  mutations: {},
  /* 비동기 처리 로직을 선언 */
  actions: {},
  modules: {}
});
